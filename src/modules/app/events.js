/**
 * App module event definitions
 * Events that the app module publishes or subscribes to
 */

export const events = {
  // App-specific events
  APP_READY: 'app/ready',
  APP_ERROR: 'app/error',
};