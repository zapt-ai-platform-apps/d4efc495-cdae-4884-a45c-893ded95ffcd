/**
 * UI module event definitions
 * Events that the UI module publishes or subscribes to
 */

export const events = {
  // UI-related events
  COMPONENT_MOUNTED: 'ui/component/mounted',
  COMPONENT_UNMOUNTED: 'ui/component/unmounted',
  THEME_CHANGED: 'ui/theme/changed',
};